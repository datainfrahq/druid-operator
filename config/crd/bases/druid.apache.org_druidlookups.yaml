---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: druidlookups.druid.apache.org
spec:
  group: druid.apache.org
  names:
    kind: DruidLookup
    listKind: DruidLookupList
    plural: druidlookups
    singular: druidlookup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DruidLookup is the Schema for the druidlookups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DruidLookupSpec defines the desired state of DruidLookup
            properties:
              druidCluster:
                type: string
              druidRouterUrl:
                type: string
              id:
                type: string
              spec:
                type: string
              tier:
                type: string
            required:
            - druidCluster
            - id
            - spec
            type: object
          status:
            description: DruidLookupStatus defines the observed state of DruidLookup
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
